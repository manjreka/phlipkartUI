const BASE_URL = process.env.BASE_URL;

const apiService = {
  getAll: async (url) => {},
  getOne: async (url) => {},
  post: async (url) => {},
  put: async (url) => {},
  delete: async (url) => {},
};

export default apiService;
